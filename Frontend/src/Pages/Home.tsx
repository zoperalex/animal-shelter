import React from "react";
import { Carousel, HomeBody, Navbar } from "../Components";

const Home = () => {
    return (
        <div>
            <Navbar />
            <Carousel />
            <HomeBody />
        </div>
    );
};

export default Home;
